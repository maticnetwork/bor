package wit

// PSP - TODO - add logic to all the handlers

func handleWitness(backend Backend, msg Decoder, peer *Peer) error {
	return nil
}

func handleWitnessAnnounce(backend Backend, msg Decoder, peer *Peer) error {
	return nil
}

func handleWitnessRequest(backend Backend, msg Decoder, peer *Peer) error {
	return nil
}

func handleWitnessCancel(backend Backend, msg Decoder, peer *Peer) error {
	return nil
}
