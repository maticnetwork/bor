# Needs-wiring / deferred backlog: go-ethereum v1.17.4 sync

Durable backlog of upstream changes intentionally **not adopted** during the
sync — features/tests that merged clean (or were deferred at a conflict) and
kept out of Bor to preserve behavior, but which the PoS team could pick up
later. Populated per batch (invariant: nothing dropped silently). Fork/EIP
enable-decisions live in `fork-register.md`; this file is for the *wiring work*
a future adoption would require.

`status` ∈ {`deferred`, `coverage-gap`, `adopted`, `wontfix`}.

| Item | source (batch / boundary) | status | why deferred | to adopt (wiring required) | pointers |
| ---- | ------------------------- | ------ | ------------ | -------------------------- | -------- |
| `--miner.maxblobs` (configurable per-block blob cap) | batch 3 (v1.17.0 3/12, `a122dbe45`) | deferred | Bor's miner is heavily diverged (`worker` struct with `w.chainConfig`, restructured `commitTransaction`, removed the isCancun blob-space-left block); it's an operator knob, not consensus. Wiring it needs adaptation to Bor's worker structure + flag→config plumbing — out of scope for a merge batch. Bor keeps the protocol-default cap (`eip4844.MaxBlobsPerBlock`), backward-compatible. | 1. add `MaxBlobsPerBlock int` to `miner.Config`; 2. add a `maxBlobsPerBlock(time)` helper reachable from Bor's `worker` (`w`), falling back to `eip4844.MaxBlobsPerBlock` when 0; 3. replace the `eip4844.MaxBlobsPerBlock(w.chainConfig, time)` call sites in `miner/worker.go`; 4. re-add `MinerMaxBlobsFlag` in `cmd/utils/flags.go`, its `setMiner` read, and the `cmd/geth/main.go` registration. | `miner/worker.go`, `miner/miner.go`, `cmd/utils/flags.go` (`setMiner`), `cmd/geth/main.go`; upstream boundary `a122dbe45` |
| `core/bintrie_witness_test.go` (binary-trie witness test) | batch 2 (v1.17.0 2/12, `cf93077fa`) | coverage-gap | Upstream's newly-added test can't compile against Bor: uses time-based `ShanghaiTime`/`VerkleTime` config fields + a `u64` helper Bor lacks, and Bor's different `InsertChain` signature. Removed to keep the tree building; Bor doesn't run the verkle/binary-trie transition. | Only if Bor adopts the binary-trie transition (see Verkle in `fork-register.md`): re-add the file and adapt it to Bor's block-based config (`VerkleBlock`/`IsVerkleGenesis()`), define/borrow the `u64` helper, and fix the `InsertChain` call to Bor's signature. | removed file (recoverable from upstream `cf93077fa`); relates to Verkle row in `fork-register.md` |
| syncModer refactor — "keep current syncmode in downloader only" (#33157) | batch 5 (v1.17.0 4/12, `228933a66`) | deferred | Upstream moved sync-mode selection into the downloader via a new `syncModer` (`eth/downloader/syncmode.go`), dropped the `mode` param from `synchronise`/`BeaconSync`/`BeaconExtend`/`BeaconDevSync`, added `(*Downloader).ConfigSyncMode()`, auto-disables snap after the first cycle, and moved the `SyncMode` **type** fully into `eth/ethconfig`. Bor's downloader is a deep fork (`bor_downloader.go`, `eth/downloader/modes.go` with Bor-only `StatelessSync`/`BytecodeOnlySnapSync`, witness sync, whitelist, fast-forward) and keeps `SyncMode` in the `downloader` package. Porting would require relocating the type (breaking Bor's extra modes' package home) and reworking Bor's mode handling — out of scope and high-risk for a merge batch; sync-mode is operational, not consensus. Kept Bor's mode-taking signatures + `h.snapSync.Load()` checks + `Ethereum.SyncMode()`; reverted `handler.go`/`syncer.go`/`beaconsync.go`/`beacondevsync.go`/`sync_test.go`/`handler_test.go`/`handler_eth_test.go` to Bor HEAD; removed the added `eth/downloader/syncmode.go`. | Decide whether Bor adopts syncModer: (1) move `SyncMode` type to `ethconfig` while preserving Bor's `StatelessSync`/`BytecodeOnlySnapSync`; (2) introduce `syncModer` adapted to `bor_downloader.go`'s `synchronise(id,hash,td,ttd,mode,beaconMode,ping)`; (3) add `ConfigSyncMode()` and re-slim `BeaconSync`/`BeaconExtend`/`BeaconDevSync`; (4) restore the auto-disable-snap-after-first-cycle behavior (currently in Bor's `enableSyncedFeatures`). | upstream boundary `228933a66`; `eth/downloader/{bor_downloader.go,modes.go,beaconsync.go,beacondevsync.go}`, `eth/{handler.go,backend.go,syncer/syncer.go,catalyst/api.go}` |
| Slow-block detailed statistics logging (#32812) | batch 5 (v1.17.0 4/12, `228933a66`) | deferred | Upstream added `--debug.logslowblock` + `ethconfig.SlowBlockThreshold` + `core.BlockChainConfig.SlowBlockThreshold` + a `core/blockchain_stats.go` (`blockProcessingStats.logSlow`/`reportMetrics`) and calls `res.stats.logSlow(block, bc.slowBlockThreshold)` in `insertChain`. The logging is entangled with upstream's `blockProcessingResult.stats` structure, which Bor's heavily-diverged `ProcessBlock` (tuple-returning, BlockSTM-V2, state-sync feed, milestone validation) does not share. Config plumbing is trivial but the actual logging can't be grafted mechanically. Deferred the whole feature: removed `core/blockchain_stats.go`, the `--debug.logslowblock` flag (`cmd/utils/flags.go` + `cmd/geth/{main,chaincmd}.go` registrations), and reverted `ethconfig/{config,gen_config}.go`, `eth/backend.go`, `core/blockchain.go` to Bor HEAD. | Adapt slow-block logging to Bor's `insertChain`: (1) re-add `ethconfig.SlowBlockThreshold` (+ default `2s`) and the `--debug.logslowblock` flag; (2) thread it into `core.BlockChainConfig` + `BlockChain.slowBlockThreshold` + backend wiring; (3) port `logSlow`/`reportMetrics` onto Bor's per-block stats (Bor uses direct `*Timer.Update(...)` calls, not a `stats` struct) or add a minimal Bor-native slow-block log gated on the threshold. Bor operators want this (see slow-block incidents), so a good adopt candidate. | upstream boundary `228933a66`; commit `042c47ce1`; `core/blockchain.go` (`insertChain`, `ProcessBlock`), `core/blockchain_stats.go` (upstream), `eth/backend.go`, `eth/ethconfig/*`, `cmd/utils/flags.go`, `cmd/geth/{main,chaincmd}.go` |
| Reader cache-stats rename / export (#33254) | batch 5 (v1.17.0 4/12, `228933a66`) | deferred | Upstream renamed `ReaderStats` fields `AccountHit`→`AccountCacheHit` (etc.), renamed the `readerWithCacheStats` internals, and added a `ReaderStats.String()` + metric export. Bor's `core/state/reader.go` carries a richer, deliberately-diverged prefetch-attribution instrumentation (role-based hit/miss, prefetch-origin attribution, unique-insert counters — the pipelined-SRC work) that uses the old names and its own fields, and `core/blockchain.go` reads them via `GetStats()` with the old names. Adopting the rename would churn Bor's instrumentation for no functional gain. Reverted `core/state/reader.go` + `core/state/statedb.go` to Bor HEAD. | If converging Bor's reader instrumentation to upstream: rename Bor's fields to the `*CacheHit`/`*CacheMiss` scheme, reconcile with Bor's prefetch-attribution counters, add the `String()`/export, and update `GetStats()` call sites in `core/blockchain.go`. Low priority — Bor's version is a superset. | upstream boundary `228933a66`; commit(s) in `core/state/reader.go`, `core/state/statedb.go`; consumers in `core/blockchain.go` |
| tx-announcement metadata validation (#33378) | batch 6 (v1.17.0 5/12, `5dfcffcf3`) | deferred | Upstream replaced the fetcher's `hasTx func(common.Hash) bool` callback with `validateMeta func(common.Hash, byte) error` (validates the announced tx type/metadata against the local txpool and drops invalid announcements), converting the `NewTxFetcher`/`NewTxFetcherForTests` signature and every call site. Bor's fetcher is diverged (`txMetadata` type vs upstream `txMetadataWithSeq`; `hasTx`-based; 34 old-signature call sites in `tx_fetcher_test.go`). Adopting means rewriting the production caller (`eth/handler.go`) + 34 test sites + the fuzzer, with subtle per-test semantic mapping (hasTx→validateMeta). Reverted `eth/fetcher/tx_fetcher.go`, `tx_fetcher_test.go`, `eth/handler.go`, `eth/handler_test.go`, `tests/fuzzers/txfetcher/txfetcher_fuzzer.go` to Bor HEAD. Security-positive (rejects malformed announcements) — worth adopting. | 1. change the callback to `validateMeta func(common.Hash, byte) error`; 2. update `NewTxFetcher`/`NewTxFetcherForTests` + the `TxFetcher` struct (`hasTx`→`validateMeta`); 3. wire the production caller in `eth/handler.go` (build the validateMeta closure over the txpool); 4. migrate all 34 test call sites + the fuzzer to the new signature; 5. adopt the announcement-loop refactor (validate → `continue` on invalid). | upstream boundary `5dfcffcf3`; commit `56d201b0f`; `eth/fetcher/tx_fetcher.go`, `eth/handler.go`, `tests/fuzzers/txfetcher/txfetcher_fuzzer.go` |
| State code-read metrics changeset (#33442, #33376, #33415, #33400) | batch 6 (v1.17.0 5/12, `5dfcffcf3`) | deferred | Upstream added `CodeReads`/`CodeLoaded` StateDB metrics (#33442), fixed contract-code metric counting (#33376) and code-existence marking (#33415, which also added `Has` to the `state.Reader` interface), plus test-readability (#33400). Entangled with Bor's diverged state package: the auto-merge silently swapped Bor's consensus-critical `CommitWithUpdate`/`stateUpdate` for upstream's `Commit`/`CommitAndTrack`, and cascaded into test-helper breaks (`recordingReader` missing `Has`, `commitAndFlush` arity). Bor also uses direct `*Timer.Update()` metrics, not upstream's `blockProcessingResult.stats` struct. Reverted `core/state/{statedb,state_object,reader,state_sizer,stateupdate}.go`, `core/blockchain.go`, and their tests to Bor HEAD. | Adopt carefully, preserving Bor's `CommitWithUpdate` and prefetch instrumentation: add `CodeReads`/`CodeLoaded` fields + their population in `state_object.go`; add `Has` to Bor's `Reader` implementers + test helpers; wire `codeReadTimer.Update(statedb.CodeReads)` into Bor's direct-timer blocks (`insertChain`/`processBlock`) and fold `CodeReads` into `blockExecutionTimer`. Relates to the #33254 row (same instrumentation surface). | upstream boundary `5dfcffcf3`; commits `ffe9dc97e`/`9a346873b`/`15f52a293`/`3a5560fa9`; `core/state/*.go`, `core/blockchain.go` |
| `BlockTest.run` inlined into `BlockTest.Run` (#33383) | batch 6 (v1.17.0 5/12, `5dfcffcf3`) | wontfix | Upstream inlined the `run` helper into `Run` and removed the separate method. Bor deliberately keeps `Run`/`RunV2`/`run(..., useV2 bool)` for V2-BlockSTM differential testing (`TestExecutionSpecBlocktestsV2`). Kept Bor's harness (took ours). | n/a — Bor's split is intentional; do not inline. | `tests/block_test_util.go`; upstream commit `c21fe5475` |
| Drop Bor's go-verkle scaffolding (adopt #33461) | batch 7 (v1.17.0 6/12, `710008450`) | **adopted** (2026-07-23, user-approved) | go-ethereum abandoned the go-verkle/go-ipa impl (#33461); Bor never enabled Verkle. Mirrored #33461 in a dedicated commit on top of batch 6 (deleted `trie/verkle*.go`, `trie/utils/verkle.go`, `cmd/geth/verkle.go`; removed `ExecutionWitness` verkle fields, `PointCache` from `state.Database`/`vm.StateDB` interfaces + implementers incl. `ParallelStateDB`; `AccessEvents` key derivation → `bintrie`; dropped go-verkle/go-ipa deps; removed `verkleCommand`). Kept the dormant Verkle fork gate + Bor's `stateless.Witness`. | **Future work: mirror upstream's follow-up verkle stub PRs as they arrive in later batches** — #33461 is the first of several; Bor now tracks upstream's removal trajectory patch-by-patch. Watch for the promised "stubs to replace code when the keeper build is detected." | `fork-register.md` Verkle notes; upstream commit `3f641dba8` |
| Snap-sync lock protection (#33428) | batch 7 (v1.17.0 6/12, `710008450`) | deferred | Adds lock protection in snap sync across `core/blockchain.go`, `eth/downloader/skeleton.go`, `beaconsync.go` — all deeply Bor-forked. Concurrency-safety fix (p2p-security relevant). Reverted those files to HEAD. | Port the lock additions into Bor's forked snap-sync paths (`skeleton.go`, `bor_downloader.go`) and `blockchain.go`; verify against Bor's BlockSTM concurrency model. | upstream boundary `710008450`; commit `bf141fbfb` |
| Stale beacon header deletion (#33481) | batch 7 (v1.17.0 6/12, `710008450`) | deferred | Fixes stale beacon-header deletion in the skeleton syncer; Bor's `skeleton.go`/`beaconsync.go` are forked. Correctness fix. Reverted to HEAD. | Port the fix into Bor's forked skeleton syncer; add the debug logging in `findBeaconAncestor`. | upstream boundary `710008450`; commit `4531bfebe` |
| Finalized-block cleanup on rewind in setHeadBeyondRoot (#33486) | batch 7 (v1.17.0 6/12, `710008450`) | deferred | Uses the resolved head block number (not the requested `head`) for the finalized-invalidation check, and reorders the head-block set after snapshot regen. Bor's `setHeadBeyondRoot` diverges (chainmu.TryLock wrapper, adjacent `SnapSyncCommitHead`); taking theirs collaterally dropped `SnapSyncCommitHead`. Reverted `core/blockchain.go` to HEAD. Correctness fix. | Port the `headBlock.Number` finalized check + head-set reorder into Bor's `setHeadBeyondRoot`, preserving `SnapSyncCommitHead` and the chainmu handling. | upstream boundary `710008450`; commit `32fea008d`; `core/blockchain.go` |
| ethstats newPayload processing-time reporting (#33395) | batch 7 (v1.17.0 6/12, `710008450`) | deferred | Adds a `NewPayloadEvent` + `newPayloadFeed` to `core.BlockChain` (Bor-forked), `Subscribe/SendNewPayloadEvent` methods, and ethstats consumption. Reverted `ethstats.go`/`core/events.go`/`blockchain_reader.go`/`api_backend.go` to HEAD and removed the auto-merged emit in `eth/catalyst/api.go`. Observability. | Add `newPayloadFeed` + `NewPayloadEvent` to Bor's `blockchain.go`, the pub/sub methods, emit in the newPayload/insert path, and wire ethstats. | upstream boundary `710008450`; commit `a8a480489`; `core/blockchain.go`, `ethstats/ethstats.go`, `core/events.go` |
| New `OnStateUpdate` state-commit hook (#33490) | batch 8 (v1.17.0 7/12, `94710f79a`) | deferred | Adds an `OnStateUpdate` tracing hook invoked after state commit, threading `update.ToTracingUpdate()` through `writeBlockWithState` and a `tracer` arg through `SetupGenesisBlockWithOverride`. Entangled with Bor's consensus-critical state divergence: it restructures `writeBlockWithState` around upstream's `CommitWithUpdate`/`CommitAndTrack` (Bor has its own `CommitWithUpdate` returning `*stateUpdate` + timing metrics + `WriteBytecodeSyncLastBlock` + `[]*types.Log` return), reworks `contractCode`/`stateUpdate` structs (`originHash`/`duplicate`/`originBlob`), and is the continuation of the state code-metrics line already deferred in batch 6 (#33442/#33376/#33415/#33400). Reverted all 14 touched files to Bor HEAD: `core/blockchain.go`, `core/genesis.go`, `core/genesis_test.go`, `core/state/{statedb,state_sizer,stateupdate,state_object}.go`, `core/tracing/hooks.go`, `cmd/geth/chaincmd.go`, `core/chain_makers.go`, `core/headerchain_test.go`, `eth/filters/{filter_system_test,filter_test}.go`, `tests/block_test_util.go`. | Adopt alongside the batch-6 state code-metrics row (same `stateUpdate`/`CommitWithUpdate` surface): thread `tracer` into Bor's `SetupGenesisBlockWithOverride`; add the `OnStateUpdate` hook + `ToTracingUpdate()` onto Bor's `stateUpdate`; graft the hook call into Bor's `writeBlockWithState` next to its existing `stateSizer.Notify`. Do the two together (they share `stateupdate.go`). | upstream boundary `94710f79a`; commit `01b39c96b`; relates to the batch-6 state code-metrics row |
| Contract-code reader cache statistics (#33532) | batch 8 (v1.17.0 7/12, `94710f79a`) | deferred | Adds hit/miss cache statistics to the contract-code reader (`core/state/{database,reader}.go`). Same reader-instrumentation surface as the batch-5 #33254 row, which Bor deliberately diverges on (richer prefetch-attribution instrumentation from the pipelined-SRC work). Reverted `core/state/database.go` + `core/state/reader.go` to Bor HEAD. | Fold upstream's code-reader cache stats into Bor's existing reader instrumentation (reconcile with the prefetch-attribution counters), same as the #33254 adoption. Low priority — Bor's instrumentation is a superset. | upstream boundary `94710f79a`; commit `9623dcbca`; relates to the batch-5 #33254 row |
| Duplicate `chainHeadFeed.Send` dedup (#33563) | batch 8 (v1.17.0 7/12, `94710f79a`) | deferred | Trivial cleanup extracting `sendChainHeadEvent()` from `SetHead`/`SetHeadWithTimestamp`. Auto-merged cleanly, but shares `core/blockchain.go` with the entangled #33490 change; blockchain.go was reverted wholesale to Bor HEAD for safety (single most consensus-critical file), so this clean cleanup was deferred with it. No behavior change either way. | Re-apply the `sendChainHeadEvent()` extraction in `SetHead`/`SetHeadWithTimestamp` (the auto-merge already produced a correct form). Trivial; adopt whenever blockchain.go is next touched. | upstream boundary `94710f79a`; commit `127d1f42b`; `core/blockchain.go` |
| KZG-proof peer-drop test (`makeInvalidBlobTx`/`TestTransactionProtocolViolation`, from #33602's KZG series) | batch 8 (v1.17.0 7/12, `94710f79a`) | wontfix | Upstream's `5b99d2bba` (drop peers on invalid KZG proofs) re-added `makeInvalidBlobTx` + a *running* `TestTransactionProtocolViolation` using the new `NewTxFetcher(func(common.Hash, byte) error, …)` signature (#33378). Bor already carries an adapted copy: the same helper + a `t.Skip("bor: not relevant, transaction type not supported")` test against Bor's retained `func(common.Hash) bool` signature. Auto-merge produced duplicate `makeInvalidBlobTx`/`TestTransactionProtocolViolation` decls; kept Bor's skipped copy, deleted upstream's duplicate. | n/a — the running test depends on adopting #33378's fetcher-signature change (see the batch-6 #33378 row). If #33378 is adopted, un-skip Bor's copy and drop the `func(common.Hash) bool` shim. | upstream boundary `94710f79a`; commit `5b99d2bba`; `eth/fetcher/tx_fetcher_test.go`; depends on the batch-6 #33378 row |

| core/vm write-protection relocation (#33281 + #33637) | batch 9 (v1.17.0 8/12, `8fad02ac6`) | deferred | Upstream moved EIP-214 write-protection enforcement out of the interpreter/opcodes into the gas handlers (#33281: `gas_table.go` gasCall, `operations_acl.go` selfdestruct gas; instructions.go −12) and added per-opcode read-only checks (#33637). Intertwined in `instructions.go`/`operations_acl.go` with #32919 (selfdestruct rework, deferred) and #33450. Bor's existing write-protection works and is tested; reverted `core/vm/{gas_table,operations_acl,instructions,interface}.go` to HEAD. Behavior-neutral (EIP-214 is unconditional). | Re-apply the gas-handler write-protection relocation onto Bor's `core/vm` once #32919 is untangled (they share instructions.go/operations_acl.go). Low priority — no behavior change. | upstream boundary `8fad02ac6`; commits `23c349883`/`500931bc8` |
| Selfdestruct cold-access gas early-return (#33450) | batch 9 (v1.17.0 8/12, `8fad02ac6`) | deferred | EIP-2929 correctness: return early in the selfdestruct gas fn if gas can't cover cold account access. `operations_acl.go` (+4), intertwined with #33281/#33637 there; reverted with the write-protection cluster. Gas-accounting fix worth adopting. | Port the early-return into Bor's `makeSelfdestructGasFn` in `operations_acl.go` alongside the write-protection relocation. Verify against Bor's gas tests. | upstream boundary `8fad02ac6`; commit `b6fb79cdf` |
| Selfdestruct tracer hooks during finalisation (#32919) | batch 9 (v1.17.0 8/12, `8fad02ac6`) | deferred | Reworks `opSelfdestruct` to explicit `SubBalance`, makes `StateDB.SelfDestruct` **void**, **removes** `SelfDestruct6780` from the `vm.StateDB` interface, adds `IsNewContract`, and fires selfdestruct tracer hooks in `Finalise`. Clashes with Bor's BlockSTM `*StateDB.SelfDestruct` (returns `uint256.Int` prevBalance + `MVWrite` instrumentation; `SelfDestruct6780` consumes the return). Reverted `core/state/{statedb,statedb_hooked,statedb_hooked_test}.go`, `core/vm/{instructions,interface}.go`, `core/tracing/{hooks,gen_nonce_change_reason_stringer}.go` to HEAD; removed the new `eth/tracers/internal/tracetest/selfdestruct_state_test.go` + `selfdestruct_test_contracts/*.yul`. | Adopt with Bor's BlockSTM in mind: keep MVWrite instrumentation, decide whether to keep Bor's returning `SelfDestruct`/`SelfDestruct6780` or converge to upstream's void + `IsNewContract`; graft the finalisation tracer-hook emission. Do alongside the #33490 hook infra (batch-8 row). | upstream boundary `8fad02ac6`; commit `715bf8e81` |
| Deterministic hook emission order in Finalise (#33644) | batch 9 (v1.17.0 8/12, `8fad02ac6`) | deferred | Sorts hook emission deterministically in `Finalise` (`statedb_hooked.go` +14). Builds on the #33490 `OnStateUpdate`/hook infra deferred in batch 8. Reverted `statedb_hooked.go` to HEAD (also carried #32919). | Adopt together with #33490 + #32919 (shared `statedb_hooked.go`); ensure the ordering is deterministic under Bor's BlockSTM. | upstream boundary `8fad02ac6`; commit `2eb1ccc6c` |
| OpenTelemetry JSON-RPC tracing (#33452) | batch 9 (v1.17.0 8/12, `8fad02ac6`) | deferred | Adds OTel tracing to the JSON-RPC server: new `internal/telemetry` package, `tracerProvider` threaded through `rpc/{client,handler,server,service}.go` (`newHandler`, `serviceRegistry.callback` → 3 returns), + otel deps. `newHandler` collides with Bor's `pool *SafePool` execution-pool divergence. Observability, not consensus. Reverted the 4 rpc files to HEAD; removed `internal/telemetry/telemetry.go` + `rpc/tracing_test.go`; otel deps dropped by `go mod tidy`. | Thread `tracerProvider` through Bor's `newHandler` (alongside `pool`), re-add `internal/telemetry`, restore `serviceRegistry.callback`'s span handling, re-add otel deps. Observability nice-to-have. | upstream boundary `8fad02ac6`; commit `a9acb3ff9` |
| Signature-length panic fix (#33647) | batch 9 (v1.17.0 8/12, `8fad02ac6`) | deferred | Changes `decodeSignature` to return an `error` (instead of `panic`) on wrong signature length, threading the error through `modernSigner.SignatureValues` + `tx_setcode.go`. Bor keeps its per-fork signer chain (`pragueSigner`/`cancunSigner`) alongside `modernSigner` and a 3-return `decodeSignature`; the fix is embedded in the modernSigner refactor. Reverted `core/types/{transaction_signing,transaction_signing_test,tx_setcode}.go` to HEAD. Bor's `decodeSignature` still panics on bad length — but only on the **signing** path (`SignatureValues`, fed crypto-generated 65-byte sigs), not the peer-facing `Sender`/recover path, so low external exploitability. | Port the `decodeSignature` error-return + `SignatureValues`/`tx_setcode.go` error handling onto Bor's signer (defensive hardening). | upstream boundary `8fad02ac6`; commit `8fad02ac6` |
| Duplicate `rpc.rangelimit` flag (#33163) | batch 9 (v1.17.0 8/12, `8fad02ac6`) | wontfix | Adds an `--rpc.rangelimit` flag + `ethconfig.RangeLimit` + a `rangeLimit` param on `NewRangeFilter` to cap `eth_getLogs` block ranges. **Bor already has this feature** (`RPCBlockRangeLimit` → `filters.Config.RangeLimit`, covering eth_getLogs *and* bor_getLogs, via Bor's own `RPCGlobalRangeLimitFlag` on the same `rpc.rangelimit` CLI name). Two flags with the same name would panic at startup. Declined upstream's duplicate: removed its flag def/setter/registration/`NewRangeFilter` param + `ethconfig.RangeLimit` field; reverted `eth/filters/{api,filter,filter_system,filter_test}.go` + `graphql/graphql.go` to HEAD; kept Bor's config/flag/gen_config. | n/a — Bor's `RPCBlockRangeLimit` is the equivalent (and broader — covers bor_getLogs). If ever converging to upstream's naming, rename Bor's field/flag and drop the duplicate. | upstream boundary `8fad02ac6`; commit `3d78da917` |
| Fetcher test-code refactor (#33610) | batch 9 (v1.17.0 8/12, `8fad02ac6`) | wontfix | Refactors `tx_fetcher_test.go` to a `newTestTxFetcher` helper built on #33378's `NewTxFetcher(func(common.Hash, byte) error, …)` signature. Bor declined #33378 (batch-6 row), keeping `func(common.Hash) bool`, so the refactor can't compile against Bor's fetcher. Reverted `eth/fetcher/tx_fetcher_test.go` to HEAD. | Adopt only if #33378 is adopted (batch-6 row); then migrate to `newTestTxFetcher`. | upstream boundary `8fad02ac6`; commit `9ba13b609`; depends on the batch-6 #33378 row |
| TrienodeHistory + NodeFullValueCheckpoint TOML marshaling (gen_config) | batch 9 (v1.17.0 8/12, `8fad02ac6`); extended batch 10 (`845009f68`) | coverage-gap | Trienode-history (#32621/#33551/#33584, batch 8) and `NodeFullValueCheckpoint` (#32727, batch 10) were **adopted** (see ledger). `eth/ethconfig/gen_config.go` was resolved to Bor's HEAD both times (declining #33163's `RangeLimit` marshaling), so neither `TrienodeHistory` (default `-1`) nor `NodeFullValueCheckpoint` (default `pathdb.Defaults.FullValueCheckpoint`, also settable via `--history.trienode.full-value-checkpoint`) is yet in the generated TOML (un)marshaling. Both function at their defaults / via CLI; only TOML round-trip is missing. | Regenerate `gen_config.go` (`go generate ./eth/ethconfig/`) at the v1.17.0 milestone chores to add `TrienodeHistory` + `NodeFullValueCheckpoint` (and confirm no `RangeLimit`). | upstream boundary `8fad02ac6`/`845009f68`; `eth/ethconfig/gen_config.go` |
| Extend the code reader statistics (#33659) | batch 10 (v1.17.0 9/12, `845009f68`) | deferred | Continuation of the batch-5 (#33254) / batch-6 (#33442 line) code-read-metrics deferrals. Upstream adds `ContractCodeReaderStats` (hit/miss + `CacheHitBytes`/`CacheMissBytes`) + `HitRate()`, reworks `ReaderStats`, changes `cachingCodeReader.GetStats()` to return the struct, adds `hitBytes`/`missBytes` atomics, and `CodeLoadBytes`/`CodeUpdated`/`CodeUpdateBytes` on `StateDB`/`blockProcessingStats`. Clashes with Bor's deliberately-diverged `PrefetchStats` / pipelined-SRC reader instrumentation (`readerWithCacheStats`, `AccountHitFromPrefetch`, unique-insert counters) and Bor's direct-`*Timer.Update()` block stats (not upstream's `stats` struct). Reverted `core/state/{reader,state_object,statedb}.go` to HEAD; kept `core/blockchain_stats.go` deleted; `core/blockchain.go` code-read timers (`codeReadTimer`/`codeReadBytesTimer`) not added. | Fold upstream's code-reader byte accounting into Bor's reader instrumentation (reconcile with the prefetch-attribution counters), add `CodeLoadBytes`/`CodeUpdated`/`CodeUpdateBytes` to Bor's stats and wire `codeReadTimer`/`codeReadBytesTimer` into Bor's direct-timer blocks. Do together with the batch-5 #33254 and batch-6 #33442 rows (same surface). Low priority — Bor's instrumentation is a superset. | upstream boundary `845009f68`; commit `c2595381b`; `core/state/{reader,state_object,statedb}.go`, `core/blockchain.go`; relates to the batch-5 #33254 + batch-6 #33442 rows |
| Standardize slow-block JSON output (#33655) | batch 10 (v1.17.0 9/12, `845009f68`) | deferred | Refines the slow-block statistic logging output format for cross-client metrics (negative `SlowBlockThreshold` = disabled default, zero = log all, positive = filter). The whole slow-block feature was deferred in batch 5 (#32812) — Bor's diverged tuple-returning `ProcessBlock` (BlockSTM-V2, state-sync feed, milestone validation) doesn't carry upstream's `blockProcessingResult.stats` struct that the logging hangs off. This format refinement kept deferred with it: reverted `cmd/utils/flags.go` (`LogSlowBlockFlag`), `core/blockchain.go` (`SlowBlockThreshold` field), `eth/ethconfig/{config,gen_config}.go` (`SlowBlockThreshold` default/field) to HEAD. | Adopt together with the batch-5 #32812 row: when slow-block logging is grafted onto Bor's `insertChain`, use the #33655 output convention (negative disabled / zero all / positive filter). | upstream boundary `845009f68`; commit `1e9dfd5bb`; relates to the batch-5 #32812 row |
| legacypool add-path alloc reduction (#33701) | batch 10 (v1.17.0 10/12, `c12959dc8`) | deferred | Upstream reworked `addTxsLocked` to take a caller-provided `errs []error` (write errors in place, `Requires len(txs)==len(errs)`, skip pre-errored txs) and return only `*accountSet`, saving one `[]error` alloc per batch. Entangled with Bor's async txpool divergence: Bor renamed `addTxsLocked`→`addTxs(txs, async bool)` (returns `([]error, *accountSet)`, threads `async` into `pool.add(tx, async)` to avoid locking, merges errors in `Add` via a `nilSlot` loop). The refactor's non-conflicting parts auto-merged and clashed with Bor's async signature (`errs` undefined, wrong return arity). #33701 was the sole batch-10 commit touching `legacypool.go`, so restored the file wholesale to Bor HEAD. | Harmonize upstream's caller-provided-errs pattern into Bor's async `addTxs`: pass a pre-sized `errs` slice through `addTxs`/`pool.add`, drop the `Add` `nilSlot` merge, and skip pre-errored slots — preserving Bor's `async` flag and lock-avoidance. Micro-opt (one slice alloc/batch); low priority. | upstream boundary `c12959dc8`; commit `bba41f807`; `core/txpool/legacypool/legacypool.go` (`Add`, `addTxs`) |
| OpenTelemetry JSON-RPC CLI wiring (#33484) + spanEnd-by-pointer fix (#33772) | batch 11 (v1.17.0 11/12, `c50e5edfa`) | deferred | Continuation of the batch-9 OTel deferral (#33452). #33484 re-adds the `internal/telemetry` package (absent at HEAD since #33452 was deferred), `internal/telemetry/tracesetup.SetupTelemetry`, the `RPCTelemetry*` CLI flags + `setOpenTelemetry`, a `node.Config.OpenTelemetry` field + `OpenTelemetryConfig`, and threads the tracer through `rpc/handler.go`'s span code; #33772 fixes that span code to pass errors by pointer. `newHandler` collides with Bor's `SafePool` execution-pool divergence, and Bor already runs its own gRPC-server OTel (`internal/cli/server/server.go`) — a separate feature. Observability, not consensus. Declined: reverted `rpc/handler.go`, `node/config.go`, `cmd/geth/main.go`, `cmd/utils/flags.go` to HEAD; removed `internal/telemetry/telemetry.go`, `internal/telemetry/tracesetup/setup.go`, `rpc/tracing_test.go`; stripped the `tracesetup.SetupTelemetry` block + import from `cmd/geth/config.go` (kept its independent #33804 influxdb error-message fix). | Adopt together with the batch-9 #33452 row: thread `tracerProvider` through Bor's `newHandler` (alongside `SafePool`), re-add `internal/telemetry` + `tracesetup`, restore the span handling in `rpc/{handler,server,service}.go`, re-add the `RPCTelemetry*` flags + `node.Config.OpenTelemetry`, and reconcile with Bor's existing server-side OTel so the two don't double-configure. Observability nice-to-have. | upstream boundary `c50e5edfa`; commits `#33484`/`#33772`; relates to the batch-9 #33452 row; `rpc/handler.go`, `node/config.go`, `cmd/geth/{main,config}.go`, `cmd/utils/flags.go`, `internal/telemetry/*` |
| OpenTelemetry newPayload tracing + ctx-threading (#33521, #33780) | batch 12 (v1.17.0 12/12, `0cf3d3ba4`) | deferred | Continuation of the OTel line (batch-9 #33452 / batch-11 #33484). #33521 threads `ctx context.Context` through the entire block-processing chain (`Processor.Process`, `BlockChain.{insertChain,ProcessBlock,insertSideChain}`, `ExecuteStateless` + all call sites incl. `eth/state_accessor.go`, `eth/api_debug.go`, `eth/catalyst/{api,simulated_beacon}.go`, `cmd/keeper/main.go`) purely to carry `telemetry.StartSpan` server spans for `engine_newPayload`; #33780 tweaks span-parent handling. Entangled with Bor's diverged signatures — Bor's `Process(block, statedb, cfg, author *common.Address, interruptCtx context.Context)` (BlockSTM interrupt ctx + author), 6-return `ProcessBlock(block, header, …)`, 5-return `ExecuteStateless(config, vmconfig, block, witness, author, consensus, diskdb)` — and the payoff is telemetry spans being declined. Reverted the whole chain to HEAD (kept Bor's Prague `Bor == nil` gating + `reportBlock` naming); removed `internal/telemetry/{telemetry,telemetry_test}.go`. | Adopt together with the batch-9 #33452 + batch-11 #33484 OTel rows: re-add `internal/telemetry`, decide a ctx-threading strategy compatible with Bor's `interruptCtx`/`author` params (either add a leading `ctx` alongside them or reuse `interruptCtx`), thread it through Bor's diverged `Process`/`ProcessBlock`/`ExecuteStateless` + call sites, and add the newPayload server span in `eth/catalyst`. Observability nice-to-have. | upstream boundary `0cf3d3ba4`; commits `#33521`/`#33780`; relates to batch-9 #33452 + batch-11 #33484; `core/{types,state_processor,blockchain,stateless}.go`, `eth/{state_accessor,api_debug,catalyst/*}.go` |
| Delayed p2p message decoding (#33835) | batch 12 (v1.17.0 12/12, `0cf3d3ba4`) | deferred | 26-file eth/snap protocol refactor: `BlockBodiesResponse`/`GetTrieNodesPacket` response fields become `rlp.RawList[...]` (decode-on-demand — limits decode work on unmatched/oversized peer messages, a DoS-hardening measure), the `p2p/tracker` API changes from `Track(peer, version, reqCode, resCode, id)` to typed `Track(req Request) error` / `Fulfil(resp Response) error` (+ `ErrNoMatchingRequest`/`ErrCodeMismatch`), the receipt interface gains `Derivable() types.DerivableList` + `EncodeForStorage() (rlp.RawValue, error)`, and every eth/snap response handler is reworked around lazy decoding. Entangled with several Bor divergences: the receipt interface's Bor-only `ExcludeStateSyncReceipt()` (state-sync consensus), Bor's `newBlockPushIntervalTimer`/`lastNewBlockPushUnix` NewBlock push metrics in `handlers.go`, the snap `requestTracker` singleton (`snap/tracker.go`), and Bor's forked downloader (`bor_downloader.go`, `bor_fetchers_concurrent_bodies.go`) + wit protocol. Security-positive but a large cross-file rework on the consensus-feeding p2p path — out of scope for a merge batch. Reverted the full footprint to HEAD (`eth/protocols/eth/*`, `eth/protocols/snap/*` incl. restoring `snap/tracker.go`, `eth/downloader/{queue,queue_test,bor_fetchers_concurrent_bodies}.go`, `eth/handler_eth{,_test}.go`, `p2p/tracker/tracker.go`, `cmd/devp2p/internal/ethtest/*`); `git rm` of `eth/downloader/downloader_test.go` (DU) + new `p2p/tracker/tracker_test.go`. | Adopt the delayed-decoding rework onto Bor's protocol layer: (1) migrate `p2p/tracker` to the typed `Track(Request)`/`Fulfil(Response)` API + update all Bor callers; (2) convert `BlockBodiesResponse`/`GetTrieNodesPacket` to `rlp.RawList` while preserving Bor's `Unpack()` semantics; (3) reconcile the receipt interface with Bor's `ExcludeStateSyncReceipt()`; (4) re-add Bor's NewBlock push metrics + snap `requestTracker` behavior on top; (5) port into Bor's forked downloader. Security-positive (DoS hardening) — good adopt candidate when the protocol layer is next revisited. | upstream boundary `0cf3d3ba4`; commit `0cba803fb` (#33835); `eth/protocols/eth/*`, `eth/protocols/snap/*`, `eth/downloader/*`, `p2p/tracker/*` |
| EraE archive-format rewrite (#32157, #33827) | batch 11 (v1.17.0 11/12, `c50e5edfa`) | deferred | 15-file restructure of `internal/era`: splits the era1 monolith into `internal/era/onedb` + a new `internal/era/execdb`, adds `internal/era/proof.go`, a `types.SlimReceipt` RLP wrapper "used for era files", a `--era.format` (`era1`/`erae`) flag, and an `era.Era`/`era.Builder`/`ReadAtSeekCloser` interface layer. Entangled with two Bor divergences: (1) upstream's `ExportHistory` is built on "TD is no longer stored in the database" and recomputes initial TD by accumulating difficulty, but Bor HEAD still reads TD via `bc.GetTd`; (2) Bor's `ImportHistory(chain, db, dir, network)` adds a `db ethdb.Database` param + an explicit `InsertHeaderChain` step upstream's signature lacks. Adopting requires reworking Bor's TD storage + history-import path — out of scope for a merge batch, and TD-adjacent (consensus-sensitive). Reverted the full footprint to HEAD (`internal/era/{era,accumulator,builder,era_test,iterator}.go`, `internal/era/eradl/eradl.go`, `cmd/era/main.go`, `core/rawdb/eradb/eradb.go`, `cmd/utils/{cmd,history_test}.go`, `cmd/geth/chaincmd.go`, `core/types/receipt.go`+test); `git rm` of new `internal/era/{onedb/reader.go,onedb/builder.go,onedb/builder_test.go,onedb/iterator.go,execdb/*,proof.go}`. | Decide whether Bor adopts the EraE format: (1) reconcile `ExportHistory` with Bor's TD-in-DB (keep `bc.GetTd`, or adopt upstream's difficulty-accumulation only if Bor drops TD storage); (2) preserve Bor's `db` param + `InsertHeaderChain` in `ImportHistory` while adding the `from func(era.ReadAtSeekCloser) (era.Era, error)` format selector; (3) bring in `onedb`/`execdb` + `proof.go` + `SlimReceipt` + `--era.format`; (4) migrate `internal/era/eradl`, `cmd/era`, `core/rawdb/eradb` to the interface layer. Operational (history archive), not consensus. | upstream boundary `c50e5edfa`; commits `#32157`/`#33827`; `internal/era/*`, `cmd/utils/cmd.go`, `cmd/geth/chaincmd.go`, `core/types/receipt.go` |

| On-chain tx check in fetcher (#33607) | batch 14 (v1.17.1 1/2, `9ecb6c4ae`) | deferred | Threads a new `chain *core.BlockChain` (nil-able) through `NewTxFetcher`/`NewTxFetcherForTests`, adds a `txOnChainCache` + `chain` field to `TxFetcher`, a `txAnnounceOnchainMeter`, and an on-chain existence check in `Notify` (skip announcing txs already on chain). Builds directly on #33378's `validateMeta func(common.Hash, byte) error` + `txMetadataWithSeq` fetcher internals, which Bor **declined** (batch-6 row) — Bor keeps `hasTx func(common.Hash) bool` + `txMetadata`. The upstream `Notify` hunk even references #33378's `err` var. #33607 was the sole batch-14 commit touching all 5 of its files, so restored `eth/fetcher/{tx_fetcher,tx_fetcher_test,metrics}.go`, `eth/handler.go`, `tests/fuzzers/txfetcher/txfetcher_fuzzer.go` wholesale to Bor HEAD. | Adopt together with the batch-6 #33378 row: once Bor's fetcher migrates to `validateMeta`/`txMetadataWithSeq`, thread the nil-able `chain` param, add `txOnChainCache` population (mark-canonical / reorg) + the `Notify` on-chain skip. Perf/DoS-positive (avoids fetching already-chained txs). | upstream boundary `9ecb6c4ae`; commit `59ad40e56` (#33607); depends on the batch-6 #33378 row |
| testing_buildBlockV1 Engine API (#33656) | batch 14 (v1.17.1 1/2, `9ecb6c4ae`) | deferred | Adds a test-only `testing_buildBlockV1` catalyst Engine-API method: new `generateParams.{forceOverrides,overrideExtraData,overrideTxs}` fields, an override branch in `generateWork` (commit override txs instead of `fillTransactions`), override plumbing in `miner/payload_building.go` + `internal/ethapi/override`, `beacon/engine/types.go` + `eth/catalyst/{api,simulated_beacon}.go` wiring, and new `eth/catalyst/api_testing{,_test}.go`. Catalyst is inherited-but-unused in Bor's production consensus, and the worker.go changes collide head-on with Bor's VEBLOP `getWork`/`getWorkReq` async restructure (Bor rewrote the whole `generateWork` region — "Sanitize recommit interval" vs upstream's fillTransactions). #33656 was the sole batch-14 commit touching all its files, so reverted the 8 modified files to Bor HEAD and `git rm`'d the 2 new files. | Adopt only if the catalyst testing API is ever needed on Bor: adapt the override fields into Bor's restructured `generateParams`/`getWorkReq`, thread the override-commit branch through Bor's async work path, and re-add the catalyst wiring. Test-only; low priority. | upstream boundary `9ecb6c4ae`; commit `e40aa46e8` (#33656) |
| Drop eth/68 protocol version (#33511) | batch 15 (v1.17.1 2/2, `16783c167`) | deferred | 695-line deletion across 16 files removing eth/68 support: unifies `StatusPacket68`/`StatusPacket69`→`StatusPacket`, drops `NewBlockHashesPacket`/eth-68 receipt handling, reworks handshake/handlers/receipt. Entangled with Bor divergences: forked downloader (`bor_downloader.go`, `bor_fetchers_concurrent_bodies.go`), wit protocol, the receipt interface's Bor-only `ExcludeStateSyncReceipt()`, Bor's NewBlock push metrics (`newBlockPushIntervalTimer`/`lastNewBlockPushUnix`), and Bor's PoA block-announcement propagation (Bor still broadcasts blocks via the eth protocol, unlike post-Merge geth). Same class as the deferred #33835. Reverted the full 16-file footprint to HEAD (`eth/protocols/eth/{handler,handler_test,handlers,handshake,handshake_test,peer,protocol,protocol_test,receipt,receipt_test}.go`, `eth/handler_eth_test.go`, `eth/downloader/skeleton_test.go`, `eth/sync_test.go`, `cmd/devp2p/internal/ethtest/{conn,suite}.go`); `git rm` of DU `eth/downloader/downloader_test.go`. Two piggybacked commits reverted with it: **#2a4527240** (handshake timeout-metrics classification, 12 lines in handshake.go) and **#cee751a1e** (flaky `TestSnapSyncDisabling68` fix, sync_test.go) — both re-adoptable independently. | Adopt the eth/68 drop onto Bor's protocol layer alongside #33835: reconcile the unified `StatusPacket` + receipt interface with Bor's `ExcludeStateSyncReceipt()`, preserve NewBlock push metrics + PoA announcement path, and port into Bor's forked downloader/wit. Re-apply #2a4527240 (metrics fix) and #cee751a1e (test) at the same time. | upstream boundary `16783c167`; commit `723aae2b4` (#33511); relates to the batch-12 #33835 row |
| Prevent state flushing in RPC / `ExecuteConfig` (#33931) | batch 16 (v1.17.2 1/4, `00540f946`) | deferred | Introduces `core.ExecuteConfig{WriteState,WriteHead,EnableTracer,MakeWitness,StatelessSelfValidation,EnableWitnessStats}` threaded through `BlockChain.ProcessBlock`, moves `StatelessSelfValidation`/`EnableWitnessStats` out of `vm.Config` into `BlockChainConfig`, gates the block-write + tracer blocks on the config, and rewrites `debug_executionWitness` (now `BlockNumberOrHash`, absorbing `ExecutionWitnessByHash`) to execute with `WriteState: false` so an RPC call no longer persists state. Bor has already forked every target: its live `ProcessBlock` is the tuple-returning `(block, parent, witness, followupInterrupt)` form at `core/blockchain.go:857` (upstream's survives only as dead `processBlock`, `// nolint : unused`), `insertChain` is restructured around Bor's direct-timer metrics, and `eth/api_debug.go` has its own `ExecutionWitness`/`ExecutionWitnessByHash` pair built on the Bor signature — which returns a statedb without persisting, so **Bor does not exhibit the bug being fixed**. Adopting would mean authoring new Bor plumbing rather than merging upstream code. Reverted `core/vm/interpreter.go`, `eth/api_debug.go`, `eth/backend.go`, `internal/web3ext/web3ext.go`, `tests/block_test_util.go` wholesale, and the #33931 hunks only in `cmd/utils/flags.go` + `core/blockchain.go` (shared with #33836 / #33708). | Adopt if Bor ever converges its block-execution entry points on upstream's: add `ExecuteConfig` to Bor's `ProcessBlock`, move the two witness flags from `vm.Config` to `BlockChainConfig` (updating `bc.cfg.VmConfig.StatelessSelfValidation` / `EnableWitnessStats` readers in `insertChain`/`processBlock`), gate Bor's write path on `WriteState`, and merge Bor's two `ExecutionWitness*` RPCs into the single `BlockNumberOrHash` form. Verify Bor's witness RPC really is non-persisting before treating it as a no-op. | upstream boundary `00540f946`; commit `6d99759f0` (#33931) |
| Engine-API miner stress harness (`miner/stress`, added by #33593) | batch 16 (v1.17.2 1/4, `00540f946`) | deferred | New 210-line `miner/stress/main.go` — a block-builder stress test driven through the Engine API (`eth/catalyst` simulated beacon) that the EIP-7778 PR added alongside the gas-accounting change. Needs three surfaces Bor doesn't have: `params.ChainConfig.AmsterdamTime` (Bor's Amsterdam gate is block-based, `AmsterdamBlock`), `BlobScheduleConfig.Amsterdam`, and `ethconfig.Config.SlowBlockThreshold` (slow-block stats deferred in v1.17.0). Catalyst is inherited-but-unused in Bor's production consensus. Removed the file. | Adopt only if a Bor-side block-builder stress harness is wanted: rewrite the fork setup against Bor's block-based gates (or drive it through Bor's PoA miner instead of the Engine API), and either adopt slow-block stats or drop the `SlowBlockThreshold` knob. Dev tool; low priority. | upstream boundary `00540f946`; commit `6d0dd0886` (#33593) |
| codedb + simplify cachingDB (#33816) | batch 17 (v1.17.2 2/4, `77e7e5ad1`) | deferred | 20-file, 526-insertion rewrite of the state database layer: new `core/state/database_code.go` (`CodeDB`, a standalone contract-code store with its own cache) and `core/state/reader_stater.go` (`ReaderStater` interface), `BlockChain.statedb *state.CachingDB` replaced by `codedb *state.CodeDB` with the `state.Database` built per call (`state.NewDatabase(bc.triedb, bc.codedb).WithSnapshot(bc.snaps)`), and the `ContractCodeReader` interface reshaped — `Code`/`CodeSize` drop their `error` return and `Has(addr, codeHash) bool` is added. Blocker: that interface is the anchor of Bor's pipelined-SRC prefetch/cache-stats instrumentation (`ContractCodeReaderStats`, `ContractCodeReaderWithStats`, `ReaderStats`, `GetStats()`, `ReadersWithCacheStats()`), whose output feeds `blockProcessingResult.stats` and Bor's per-block Datadog metrics; upstream drops the concrete-typed `GetStats()` in favour of a `state.ReaderStater` type assertion. Adapting means rewriting Bor's cache-stats reader layer plus its parallel/BlockSTM `StateDB` interactions — authoring new Bor code on a state-root-determinism-critical path, not merging upstream's. Direct successor to the batch-6 state code-read-metrics row (same surface). Reverted the full footprint to HEAD; `git rm` of `core/state/{database_code,reader_stater}.go` and `core/blockchain_stats.go` (DU); in `core/blockchain.go` (shared with #33657) reverted only the #33816 hunks, **including two that auto-merged silently** — the `statedb *state.CachingDB` field being replaced by `codedb *state.CodeDB`, and the removal of the snapshot re-init in `setupSnapshot`. | Adopt together with the batch-6 state code-read-metrics row: (1) decide whether Bor keeps its own cache-stats reader layer or migrates to `state.ReaderStater`; (2) port `CodeDB` + the per-call `state.Database` construction while preserving Bor's `CommitWithUpdate`/`stateUpdate` and snapshot re-init; (3) drop the `error` returns from Bor's `Code`/`CodeSize` implementers (incl. the parallel/BlockSTM readers and test helpers) and add `Has`; (4) re-verify prefetch/process cache-stat metrics still populate. Perf/structure cleanup, not consensus — but touches state-root-determinism code, so it needs its own review + differential testing. | upstream boundary `77e7e5ad1`; commit `91cec92bf` (#33816); relates to the batch-6 state code-read-metrics row |
| Stale-pivot detection removal in processSnapSyncContent (#33150) | batch 17 (v1.17.2 2/4, `77e7e5ad1`) | wontfix | Upstream deletes the "pivot became stale, moving" block from `eth/downloader/downloader.go`, arguing it is redundant because a stale pivot is already detected by `fetchHeaders` on the beacon-header-driven path. Bor deleted upstream's `downloader.go` long ago and runs a forked downloader; the equivalent block still lives at `eth/downloader/bor_downloader.go:2048` with Bor-specific handling (`newPivotNum`). Bor's snap sync is not beacon-driven, so upstream's redundancy argument does not transfer. Kept Bor's file deletion (DU) and left the forked downloader untouched; the companion `eth/catalyst/api.go` hunk auto-merged (catalyst unused in Bor). | n/a for a merge batch — if Bor ever wants the simplification it needs its own analysis of whether `fetchHeaders` covers stale pivots on the Bor sync path, plus snap-sync soak testing. | upstream boundary `77e7e5ad1`; commit `27c4ca9df` (#33150); `eth/downloader/bor_downloader.go` |
| Miner OpenTelemetry spans / ctx-threading (#33773) | batch 18 (v1.17.2 3/4, `e23b0cbc2`) | deferred | 23-file change threading `ctx context.Context` through `consensus.Engine` (`FinalizeAndAssemble`, `Finalize`), the txpool `SubPool` interface (`Pending`), `miner.{BuildPayload,generateWork,commitWork}`, `eth/handler`, `eth/sync`, `eth/api_backend`, `internal/ethapi/simulate.go` and the whole `eth/catalyst` surface (incl. simulated-beacon server spans for `geth --dev`), purely so OpenTelemetry spans can be attached to the block-building path. Third instance of the same class (batch-9 #33452, batch-11 #33484, batch-12 #33521): Bor's OTel stack is deferred, so there is nothing for the spans to attach to, and the signature changes collide with Bor's VEBLOP miner restructure (`getWorkCh`/async work path, `newWorkReq`) and Bor's 4-return `FinalizeAndAssemble(chainHeadReader, header, state, body, receipts) (block, receipts, _, err)`. Reverted all 23 files to HEAD; `git rm` of `eth/catalyst/witness.go` + `miner/payload_building_test.go` (DU, already deleted at HEAD). | Adopt with the rest of the OTel line (batch-9/11/12 rows): once `internal/telemetry` is back and a ctx-threading strategy compatible with Bor's `interruptCtx`/`author` params is chosen, add the leading `ctx` to Bor's consensus/txpool/miner signatures and attach the block-building spans. Observability only. | upstream boundary `e23b0cbc2`; commit `98b13f342` (#33773); relates to the batch-9 #33452, batch-11 #33484 and batch-12 #33521 rows |
| Call-variant gas measurement rework (#33648) | batch 18 (v1.17.2 3/4, `e23b0cbc2`) | deferred | Splits the inner gas calculation of CALL/CALLCODE/DELEGATECALL/STATICCALL into a stateless component (memory expansion, value transfer, EIP-2929 cold access) and a stateful one (`Empty`/`Exist` account probe, EIP-7702 delegation resolution), inserting an early `if contract.Gas < intrinsic { return ErrOutOfGas }` so a call that cannot pay never reads state — required by EIP-7928 (block access lists), which brings state reads into consensus. Gas-equivalent (identical components and totals; both orderings end in error-plus-all-gas-consumed when the caller is short) but it **reorders state reads** and is **not fork-gated**, so on Bor it shifts witness contents and the BlockSTM MVHashMap read set in out-of-gas cases — deterministic, but an unconditional change to the consensus-critical EVM gas path whose only beneficiary (BAL) is dormant on Bor. It also lands in `core/vm/{gas_table,operations_acl}.go`, the same pair already carrying three deferred upstream changes (#33281, #33637, #33450, blocked behind #32919), where Bor's `gasCall` has no `readOnly` check and `gasCallEIP7702` is a plain alias rather than upstream's BAL-motivated wrapper. Reverted `core/vm/{gas,gas_table,operations_acl}.go` to HEAD. | Adopt as part of a single coordinated `core/vm` catch-up together with #33281/#33637/#33450/#32919, ideally when the PoS team decides on EIP-7928/BAL: take upstream's `makeCallVariantGasCost` + `gas*Intrinsic` split and the reworked `makeCallVariantGasCallEIP7702`, then re-verify Bor's BlockSTM read-set determinism and witness output at the out-of-gas boundary. Prerequisite for the M5/M6 BAL batches. | upstream boundary `e23b0cbc2`; commit `fd859638b` (#33648); relates to the batch-9 write-protection cluster rows |
| History pruning configuration refactor (#34036) | batch 18 (v1.17.2 3/4, `e23b0cbc2`) | deferred | Introduces `history.HistoryPolicy` (captures user *intent*) alongside the persisted prune point (actual db tail), replaces `BlockChainConfig.ChainHistoryMode` with `HistoryPolicy`, rewrites `initializeHistoryPruning` around `policy.Target`, and relaxes the semantics (an externally pruned db now warns instead of refusing to start). `eth/backend.go` constructs it via `history.NewPolicy(config.HistoryMode, genesisHash)`. Bor's `eth/backend.go` never calls `core.LoadChainConfig` — it takes `config.Genesis.Config` directly and uses a Bor-specific `CreateConsensusEngine(config.Genesis.Config, config, chainDb, blockChainAPI, vmCfg)` — so no `genesisHash` is in scope, and supplying one is new Bor plumbing for a feature Bor doesn't use (no Polygon genesis hash appears in `MergePrunePoints`/`PraguePrunePoints`, so Bor nodes are always `KeepAll`). Reverted `cmd/geth/chaincmd.go`, `cmd/workload/testsuite.go`, `core/blockchain_test.go`, `core/history/historymode.go`, `eth/backend.go`; `git rm` of the new `core/history/historymode_test.go`; `core/blockchain.go` restored to HEAD (shared with #34039). | Adopt if Bor ever wants history expiry: derive the genesis hash in Bor's `New()` (either add a `core.LoadChainConfig` call or reuse the already-loaded genesis), build the policy, swap `ChainHistoryMode` → `HistoryPolicy` in `BlockChainConfig`, and take upstream's `initializeHistoryPruning`. Operational, not consensus. Supersedes the #33657 Prague-prune-points version adopted in batch 17. | upstream boundary `e23b0cbc2`; commit `6ae3f9fa5` (#34036) |
| Stateless code-database initialization fix (#34011) | batch 18 (v1.17.2 3/4, `e23b0cbc2`) | deferred | One-line fix in `core/stateless.go` binding the statedb's code source to the stateless input: `state.NewDatabase(triedb.NewDatabase(memdb, …), state.NewCodeDB(memdb))` instead of a nil code reader. `state.CodeDB` is introduced by #33816, deferred in batch 17, so the fix cannot be applied on its own. Kept Bor's form (which additionally passes `diskdb` to `witness.MakeHashDB`, a Bor divergence). | Re-apply automatically when the #33816 codedb row is adopted — it is the same one-line change, preserving Bor's `MakeHashDB(diskdb)` argument. | upstream boundary `e23b0cbc2`; commit `a7d09cc14` (#34011); blocked on the batch-17 #33816 row |
| eth/fetcher chain-event unsubscribe nil-guard (#33950) | batch 16 (v1.17.2 1/4, `00540f946`) | coverage-gap | Wraps the `defer sub.Unsubscribe()` of the fetcher's chain-event subscription in `if sub != nil` to de-flake a test. Bor has no such subscription — the `headEventCh`/`SubscribeChainEvent` block arrived with #33378, which Bor declined (batch-6 row) — so there is nothing to guard. Kept Bor's loop. | Re-apply automatically when the batch-6 #33378 row is adopted; the guard is one `if` around the existing defer. | upstream boundary `00540f946`; commit `344ce84a4` (#33950); depends on the batch-6 #33378 row |
| History import batched insertion (#33894) | batch 19 (v1.17.2 4/4, `be4dc0c4b`) | deferred | Rewrites `ImportHistory`'s per-block `InsertReceiptChain` into an `importBatchSize` accumulator with a `flush()` closure, adds `defer e.Close()`, and reworks the checksum loop. Six hunks conflicted because Bor's copy of the function is independently diverged from upstream: it iterates via `era.NewIterator(e)` rather than `e.Iterator()`, carries a `forker`, and inserts headers through `chain.HeaderChain().InsertHeaderChain` before the receipt chain — none of which upstream does. Throughput-only change on an offline CLI path (`geth import-history`) with no consensus surface; hand-merging batched flush semantics into Bor's header-inserting loop risks silently producing a corrupt imported history db for no benefit Bor currently needs. Reverted `cmd/utils/cmd.go` to HEAD. | Adopt as part of a broader era catch-up together with the #32157 EraE row: first reconcile Bor's `ImportHistory` with upstream's current shape (iterator, no forker, header handling), then take the batching on top and verify an imported db against a known-good head. Tooling only. | upstream boundary `be4dc0c4b`; commit `8f9061f93` (#33894); relates to the batch-12 #32157 EraE row |
| OpenTelemetry SampleRatio IsSet guard (#34062) | batch 19 (v1.17.2 4/4, `be4dc0c4b`) | coverage-gap | Wraps `tcfg.SampleRatio = ctx.Float64(RPCTelemetrySampleRatioFlag.Name)` in `if ctx.IsSet(...)` so an unset flag no longer clobbers a configured value. Bor has no `setOpenTelemetry` function at all — the whole OTel flag block was declined with the batch-9/11 OTel rows — so there is nothing to guard. Kept Bor's `cmd/utils/flags.go`. | Re-apply automatically when the OTel line is adopted; it is one `if` around the existing assignment. | upstream boundary `be4dc0c4b`; commit `745b0a8c0` (#34062); depends on the batch-9 #33452 / batch-11 #33484 rows |
| Slot number in test payload (#34094) | batch 19 (v1.17.2 4/4, `be4dc0c4b`) | coverage-gap | Adds `slotNum: args.SlotNum` to the `generateParams` literal inside `miner.BuildTestingPayload`. Bor deleted `BuildTestingPayload` along with the `testing_buildBlockV1` RPC (#33656, declined in batch 12), so the only changed line has no home. Kept Bor's `miner/payload_building.go`. | Re-apply automatically when the #33656 row is adopted; it is one field in the params literal. | upstream boundary `be4dc0c4b`; commit `acdd13971` (#34094); depends on the batch-12 #33656 row |
| Default cache size bump — 1024 MB → 4096 MB (#33836 / #33975) | v1.17.2 milestone triage (batches 16, 17) | **merged clean, no effect on Bor — decision needed** | Upstream raised the total default cache from 1024 MB to 4096 MB, which lands in `eth/ethconfig.Defaults` as `DatabaseCache 512→2048`, `TrieCleanCache 154→614`, `TrieDirtyCache 256→1024`, `SnapshotCache 102→409`. Both commits merged without conflict — but **Bor's production startup path never reads those defaults**. `internal/cli/server/config.go` defines its own `Cache: 1024` with a 50/15/25/10 `PercDatabase`/`PercTrie`/`PercGc`/`PercSnapshot` split, and `calcPerc` overwrites all four `ethconfig` values in `fillEthConfig`. That split against 1024 MB reproduces geth's **old** defaults exactly (512 / 153 / 256 / 102), which is evidently how it was tuned — so upstream's bump silently leaves `bor server` 4× below upstream's new intent while `cmd/geth`, tests, and any `ethconfig.Defaults` consumer get the new values. | One-line change if the team wants parity: `Cache: 1024` → `Cache: 4096` in the `internal/cli/server` defaults (the existing percentage split then reproduces upstream's new numbers exactly, 2048 / 614 / 1024 / 409). **Deliberately not taken here** — it quadruples the default memory footprint of every Bor node, which is a PoS/devops call, not a merge decision. Note that pos-ops pins `cache` per host in each BP's `bor/config.toml`, so deployed BPs are unaffected either way; this only moves the default for operators who don't set it. | upstream boundary `00540f946` (batch 16) and `77e7e5ad1` (batch 17); commits `28dad943f` (#33836), `b8a3fa7d0` (#33975) |

## Notes

- Fork-schedule adoptions (EIP-8024, Verkle, and the upcoming Osaka/BPO/
  Amsterdam EIPs listed in `fork-register.md`) are **not** in this file's scope
  as line items — enabling any of them is a deliberate fork-schedule change
  (its own PR + N-1/N/N+1 tests), tracked in `fork-register.md`. This backlog
  is for the surrounding *code wiring* (flags, config plumbing, tests, adapters)
  a future adoption would need.
